# Auto-provisions the Prometheus datasource the dashboard panels reference
# (uid: prometheus). Grafana loads everything under /etc/grafana/provisioning.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
